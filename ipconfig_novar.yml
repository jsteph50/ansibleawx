- name: This is a simple test playbook
  hosts: all
  gather_facts: False
  vars:
    ansible_port = 5986
  tasks:
    - name: run ipconfig
      raw: ipconfig
      register: ipconfig
    - debug: var=ipconfig
